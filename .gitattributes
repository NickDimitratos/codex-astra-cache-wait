# Unified-diff blank context lines contain a required leading space.
# Check whitespace on the applied upstream source, not this patch container.
# Checksummed patch bytes must survive Windows Git line-ending conversion.
patches/*.patch -text -whitespace
plugins/astra-runtime-manager/resources/patches/*.patch -text -whitespace
